# 2026-07-27

## 数据库迁移：统一为 Turso libSQL

### 变更概要
- 项目数据库已全面统一为 `@libsql/client` + `drizzle-orm/libsql`
- 移除了旧的 better-sqlite3 残留（`src/lib/db.ts`、`src/lib/schema.ts` 已不存在）
- 清理了 `data/app.db`（旧 better-sqlite3 数据库文件）

### 环境变量
- `DATABASE_URL` 区分本地/生产：
  - 本地：`file:dev.db`（libSQL 本地文件）
  - 生产：`libsql://xxx.turso.io`
- `DATABASE_AUTH_TOKEN`：本地无需设置，生产必填
- 已移除 `TURSO_DATABASE_URL` / `TURSO_AUTH_TOKEN`（已无代码引用）

### 迁移系统
- 重新生成迁移文件：`drizzle/0000_greedy_aaron_stack.sql`，10 列匹配当前 schema
- 迁移脚本 `scripts/migrate.ts` 已添加内置 .env 加载（手动解析，无外部依赖）
- 命令：
  - `npm run db:migrate` — 生成 + 应用
  - `npm run db:migrate:generate` — 仅生成
  - `npm run db:migrate:run` — 仅应用

### 测试结果
- TypeScript: 零错误
- Vitest: 16/16 测试通过（cleanup 时安全删除锁文件不影响结果）
- Next.js build: 10/10 页面生成成功
- DB CRUD 直接测试：全部通过

### 文档更新
- README.md：`TURSO_DATABASE_URL` → `DATABASE_URL`，移除"两套 DB"限制说明
- MEMORY.md：更新技术栈描述，移除"已知架构问题"
- .env.example：添加 Turso 创建指引，完善注释
- .gitignore：添加 `dev.db` / `dev.db-*`

### 旧文件
- `local.db` 仍存在（被进程锁定，无法删除），但已不再使用
- 新数据库文件为 `dev.db`

## GitHub 推送
- 生成 ed25519 SSH 密钥，添加到 GitHub
- 远程切换为 SSH：`git@github.com:HuangYongLe/article_abstract.git`
- 提交 `a624cac`（34 files, +1446/-546）推送成功
- `.gitignore` 添加 `nul`（Windows 保留设备名，Git 无法索引）

## Vercel 构建修复
- 问题：构建时 `generateStaticParams()` 调用 DB → `DATABASE_URL` 未设置 → 模块顶层抛异常
- 修复 `src/lib/db/index.ts`：改用 Proxy 惰性初始化，仅在首次实际访问时才创建连接
- 修复 `history/[id]/page.tsx`：`generateStaticParams` 捕获异常返回 `[]`
- 修复 `sitemap.ts`：同捕获异常，DB 不可用时仅生成静态页面条目
- 验证：无 DATABASE_URL 下 `next build` 10/10 成功，`vitest` 16/16 通过，`tsc` 零错误
- 提交 `06ebb84` 已推送
